/* WAP to enter an element at specific position into array. */
#include <iostream>
using namespace std;
class Node {
public:
  int data;
  Node *next;
  Node(int a) {
    data = a;
    next = NULL;
  }
};
Node *create_Node(Node *head) { Node *a = new Node(a); }
